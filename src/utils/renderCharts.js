export const renderTodayChartData = (labels, data) => {
  return {
    labels: labels,
    datasets: [
      {
        data: data,
        backgroundColor: "#5f6678",
        hoverBackgroundColor: "#fff",
        borderRadius: 8,
        maxBarThickness: 30,
      },
    ],
  };
};

export const renderSevenDaysChartData = (labels, data) => {
  return {
    labels: labels,
    datasets: [
      {
        data: data,
        backgroundColor: "#5f6678",
        hoverBackgroundColor: "#fff",
        borderRadius: 8,
        maxBarThickness: 30,
      },
    ],
  };
};

export const renderThirtyDaysChartData = (labels, data) => {
  return {
    labels: labels,
    datasets: [
      {
        data: data,
        backgroundColor: "#5f6678",
        hoverBackgroundColor: "#fff",
        borderRadius: 8,
        maxBarThickness: 30,
      },
    ],
  };
};

export const renderNinetyDaysChartData = (labels, data) => {
  return {
    labels: labels,
    datasets: [
      {
        data: data,
        backgroundColor: "#5f6678",
        hoverBackgroundColor: "#fff",
        borderRadius: 8,
        maxBarThickness: 30,
      },
    ],
  };
};

export const renderOneYearChartData = (labels, data) => {
  return {
    labels: labels,
    datasets: [
      {
        data: data,
        backgroundColor: "#5f6678",
        hoverBackgroundColor: "#fff",
        borderRadius: 8,
        maxBarThickness: 30,
      },
    ],
  };
};
